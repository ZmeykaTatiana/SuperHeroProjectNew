package by.itstep.zmeyka.superheroproject.model.entity.container;

import by.itstep.zmeyka.superheroproject.model.entity.abstracts.Hero;

public interface GeneralTeam {
    int size();
    Hero get(int index);
}
