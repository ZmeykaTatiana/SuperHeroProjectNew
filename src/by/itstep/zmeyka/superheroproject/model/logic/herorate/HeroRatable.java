package by.itstep.zmeyka.superheroproject.model.logic.herorate;

import by.itstep.zmeyka.superheroproject.model.entity.abstracts.Hero;

public interface HeroRatable {
     boolean compare(Hero first, Hero second);
}
